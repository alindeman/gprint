# gprint

Google Print CLI.

Extremely early/experimental.

## delete-jobs

Delete all jobs:

```bash
gprint \
  --credentials-file=credentials.json \
  --token-file=token.txt \
  delete-jobs --all
```
